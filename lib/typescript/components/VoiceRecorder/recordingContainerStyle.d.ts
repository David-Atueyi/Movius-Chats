import { ViewStyle } from 'react-native';
import { RecordingUIProps, VoiceRecorderStyleOverrides } from '../../types';
/** Shared wrapper styles for every built-in recording mode. */
export declare function getRecordingContainerStyle(voiceRecorderStyles?: VoiceRecorderStyleOverrides, recordingUIProps?: RecordingUIProps): ViewStyle;
